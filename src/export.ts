import type { UserConfig } from './types'

export type * from './types'

/**
 *
 * Config file path `release.conf.ts`
 *
 * @param conf
 * @returns
 */
export function defineConfig(conf: UserConfig) {
  return conf
}
